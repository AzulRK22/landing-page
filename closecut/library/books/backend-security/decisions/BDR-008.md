# BDR-008 - Idempotent Remote Writes

**Status:** Accepted

## Context

CloseCut requires a stable platform decision for idempotent remote writes.

## Decision

Stable document IDs and set/merge semantics are required so retries do not duplicate logical operations.

## Consequences

All implementation and migration work must preserve this contract until superseded by an approved record.
