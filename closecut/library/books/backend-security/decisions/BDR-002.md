# BDR-002 - Firestore as Remote Sync Layer

**Status:** Accepted

## Context

CloseCut requires a stable platform decision for firestore as remote sync layer.

## Decision

Firestore is the remote replication and trusted-sharing layer, not a direct replacement for local application state.

## Consequences

All implementation and migration work must preserve this contract until superseded by an approved record.
