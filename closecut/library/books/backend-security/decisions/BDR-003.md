# BDR-003 - Local Writes First

**Status:** Accepted

## Context

CloseCut requires a stable platform decision for local writes first.

## Decision

User mutations commit locally before remote synchronization. Remote failure must not erase a valid local action.

## Consequences

All implementation and migration work must preserve this contract until superseded by an approved record.
